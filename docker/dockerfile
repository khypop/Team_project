FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
# FastAPI 서버를 8000포트로 실행
CMD ["uvicorn", "fastapi_signaling:app", "--host", "0.0.0.0", "--port", "8000"]